# Installed Skill Memory

The complete skill files are stored at `/home/user/arena-design-skills` and archived as `arena-design-skills-current.tar.gz`.

## Activation policy

For design/UI/frontend/diagram/accessibility/visual/video work:

1. read `/home/user/arena-design-skills/SKILL.md`;
2. run or apply `/home/user/arena-design-skills/route.py`;
3. select only the smallest matching skill set;
4. read every selected skill's `SKILL.md`;
5. tell the Owner which skills activated and why;
6. execute their completion gates;
7. honor explicit opt-out.

Do not activate design skills for pure backend/database/infrastructure work without a design signal.

## Curated skills

1. `excalidraw` — editable diagrams, architecture flows, wireframes, SVG previews.
2. `penpot` — open design-system handoff, vectors, tokens, components.
3. `onlook` — browser-guided component/UI source improvement.
4. `shadcn-ui-engine` — accessible composable UI components in the host stack.
5. `assistant-generative-ui` — streaming assistant/tool/approval interfaces.
6. `design-md-tokens` — DESIGN.md plus synchronized CSS/JSON tokens.
7. `ui-ux-pro-max` — full hierarchy/color/type/spacing/motion/responsive audit.
8. `taste-skill` — aesthetic quality gate against generic AI UI habits.
9. `hallmark` — post-build professional UI remediation.
10. `axe-core-a11y` — WCAG 2.2 AA automated/manual accessibility QA.
11. `visual-regression-qa` — deterministic multi-viewport screenshot baselines/diffs.
12. `remotion-best-practices` — deterministic React-based video planning/render QA.
13. `understand-figma` — user-authorized Figma export knowledge graph.

## Ticket 07 design skills used

- `onlook`
- `axe-core-a11y`
- `visual-regression-qa`

Ticket 07 browser acceptance retained desktop/mobile baselines and reported zero serious axe violations.

## Important meaning of “installed”

These are lightweight, independently authored Arena workspace instruction wrappers. Upstream applications were not cloned or silently bundled. Any future package/service installation requires explicit approval, exact version pinning, license/security review, and remote-first/development-only treatment when heavy.
