---
status: accepted
---
# Install Hub packages as verified drafts

One-click installation downloads and verifies a Workflow Package, presents permission, dependency, and credential differences, imports it as a Mutable Draft, and runs fixtures in a sandbox before credentials can be filled and publication considered. Internet content therefore reaches the canvas and project list easily without turning discovery into immediate remote-code execution.
