---
status: accepted
---
# Enforce hard agent budgets

Agent Policy imposes hard limits on tokens, money, wall time, tool calls, recursion, memory retrieval, and output size, with optional cheaper or local fallback and approval for a larger budget. Usage and decisions enter redacted traces, but private model reasoning and credentials are neither required nor persisted.
