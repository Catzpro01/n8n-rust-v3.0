---
status: accepted
---
# Automate only safe remediation by default

The default remediator may perform reversible operational actions such as classified retry, checkpoint resume, token refresh, circuit breaking, worker relocation, and batch reduction. Changes to workflow definitions, code, credentials, or publication state are emitted as auditable Remediation Patches requiring approval; an autonomous policy may be enabled per workflow with explicit action and cost limits.
