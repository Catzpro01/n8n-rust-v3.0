---
status: accepted
---
# Make scraping compliant by default

Crawl Plans honor robots rules, target scope, rate limits, data minimization, and recorded terms by default and require an explicit reason and approval for a permitted override. Login and private resources require scoped credentials, and no adapter may bypass access the owner does not possess; flexibility must not silently become unauthorized access.
