---
status: accepted
---
# Support scoped GitHub authentication modes

GitHub integration prefers a GitHub App with repository-scoped permissions and also supports OAuth device or web flow and fine-grained personal access tokens. The Connector Definition covers common repository, content, branch, commit, issue, pull request, Actions, release, webhook, GraphQL, and custom operations while reusing only the explicitly granted credential scopes.
