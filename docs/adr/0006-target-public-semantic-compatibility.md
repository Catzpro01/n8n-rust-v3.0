---
status: accepted
---
# Target public semantic compatibility rather than bug-for-bug parity

Compatibility covers user-visible workflow JSON import/export, common expressions, webhooks, credential references, core-node behavior, and delegated n8n packages, while internal database interfaces and known n8n bugs are explicitly out of scope. Every import must produce a Compatibility Report before activation so behavioral differences are visible rather than guessed.
