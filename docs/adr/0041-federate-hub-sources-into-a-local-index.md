---
status: accepted
---
# Federate Hub sources into a local index

Workflow Hub maintains a local searchable index over owner-curated catalogs, GitHub repositories and releases, manually supplied URLs, and compatible n8n template-library endpoints. Package data is fetched from pinned origins, avoiding a required central marketplace while keeping a Play-Store-like discovery experience on the self-hosted instance.
